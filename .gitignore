# Node modules folder (this is where your dependencies are installed)
node_modules/

# Environment variables file (it contains sensitive data like API keys or database credentials)
.env

# Logs (log files generated by the app)
*.log

# OS-specific files (system files created by macOS, Windows, etc.)
.DS_Store
Thumbs.db

# Build output (if you're using a build process, this may be needed to be ignored)
dist/
build/

# npm debug files
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Optional: Ignore coverage directory if using code coverage tools
coverage/

# Optional: Ignore any other generated files or directories (like temp files)
*.tgz

# IDE or editor settings (you can add your editor's configuration folder here)
.vscode/
.idea/
*.swp
